# DESIGN.md — デザインの設計思想

このテンプレートは「**AI にドキュメントを HTML で書かせる**」ことだけを目的に設計している。
人間が手で書く HTML なら許される自由度を、あえて捨てている箇所がいくつもある。
その取捨選択の理由をここに残す。

---

## 0. 前提として置いた制約

| 制約 | 内容 |
| --- | --- |
| 書き手は AI | 記憶に頼らせない。クラス名を覚えていなくても破綻しない構造にする |
| 読み手は人間 | 技術ドキュメント／レポートを、画面でも紙でも読む |
| 出力は使い捨てになりうる | ビルド工程やパッケージ依存を前提にしない。ブラウザで開けば動く |
| 配布経路が読めない | 社内 Wiki 貼り付け、Slack 添付、メール、印刷、いずれもありうる |

---

## 1. セマンティック HTML を第一級に置く

**原則: `<h2>` と書けば見出しになる。クラスは足さなくてよい。**

スタイルはほぼすべて要素セレクタで当てている。
`.doc-body h2`、`.doc-body table`、`.doc-body blockquote` のように、
「文書の中に置かれた素の HTML」がそのまま整うようにした。

これは AI に書かせるうえで決定的に効く。生成モデルは長い文書を書くほど
独自クラス名の記憶が薄れ、途中から `<div class="section">` のような
自己流の構造を混ぜ始める。要素セレクタなら、そうなっても壊れない。

クラスを要求するのは、**HTML の語彙では表現できない意味**だけに絞った。

| クラス | 存在理由 |
| --- | --- |
| `.callout` + `.callout-warn` など | 「注意」「補足」に対応する HTML 要素が存在しない |
| `.badge` | ステータスラベルという概念が HTML にない |
| `.lead` | 「冒頭の要約」を `<p>` から区別できない |
| `.doc-meta` | メタ情報の `<dl>` を本文中の `<dl>` と区別する必要がある |
| `.card` / `.doc-grid` | 並列配置はレイアウトの指示であって意味論ではない |

**覚えるべきクラスは実質 5 つ**。これ以上増やさないことを設計上の上限とした。

### なぜ `.doc-body` というスコープを 1 枚かませたか

素の `h2 { … }` にすると、目次サイドバーやヘッダの見出しにも当たってしまう。
「本文の中の要素だけ」を対象にするために、記事全体を `.doc-body` で囲う 1 段だけ許した。
AI が書くのはこの中身だけなので、負担は 1 回きりで済む。

---

## 2. 見た目の決定をすべてトークンに集約する

色・余白・タイプスケール・角丸はすべて `:root` の CSS カスタムプロパティに置いた。
CSS の本文中に生の色コードは（ハイライト定義を除き）現れない。

これは 2 つの効果を狙っている。

1. **ダークテーマが「別スタイル」にならない。**
   ダークモードは `--doc-c-bg` 以下 15 個ほどの変数を差し替えるだけで完結する。
   レイアウトやコンポーネントの定義は 1 つも複製していない。
2. **組織ごとの色替えが 1 ファイルの上部だけで終わる。**
   `--doc-c-accent` を差し替えれば、リンク・目次の現在位置・見出しの強調・
   `.badge-info` の枠線まで一斉に追随する。

余白は 4px 基準の 8 段階（`--doc-space-1` 〜 `-8`）に固定した。
段階を絞ることで、AI が生成した文書でも縦のリズムが崩れにくい。

### テーマ判定を三段構えにした理由

```
:root                                        → ライト（既定）
@media (prefers-color-scheme: dark)
  :root:not([data-theme="light"])            → OS がダークなら追随
:root[data-theme="dark"]                     → 明示指定が最優先
```

OS 追従だけだと「ダークな環境で明るい配色の資料を人に見せたい」ができない。
明示指定だけだと初期表示が環境に合わない。両立させるにはこの順序が必要になる。

---

## 3. 日本語の可読性を最優先する

英語圏のドキュメントテーマをそのまま使うと日本語は必ず読みにくくなる。
以下は日本語混植を前提に個別に決めた値である。

**行送り 1.85。** 英文向けの 1.5〜1.6 では、仮名と漢字が詰まって行が識別しづらい。
漢字仮名交じり文はアセンダ・ディセンダによる行の凹凸がないため、
行間で行を分ける必要がある。見出しだけは 1.45 に締めて塊として見せる。

**本文幅 46rem（約 736px）。** 全角で 40 文字前後。
日本語は 1 行 35〜45 文字が読みやすいとされ、この範囲に収まる。
横に広げると行の戻り先を見失う。

**折り返しの制御。**

```css
word-break: normal;       /* 日本語を任意の位置で折らない */
overflow-wrap: anywhere;  /* 長い URL や識別子だけは折る */
line-break: strict;       /* 禁則処理を厳格に */
```

`word-break: break-all` は使わない。日本語が単語の途中で折れて読みにくくなる。
一方で `overflow-wrap: anywhere` がないと、長い英語の識別子が
本文幅を突き破ってレイアウトを壊す。この 2 行はセットで意味を持つ。

**見出しにだけ `font-feature-settings: "palt"`。**
プロポーショナル詰めは見出しでは締まって見えるが、
本文に掛けると読み進める際のリズムを損なう。適用範囲を見出しに限定した。

**フォントは system-ui 起点。** Web フォントを読みに行かない。
社内ネットワークやオフラインでも表示が変わらないことを、
書体の理想形より優先した。明朝で読ませたい報告書のために
`<html data-doc-font="serif">` の逃げ道だけ用意してある。

---

## 4. JavaScript は「あれば良くなる」層に限定する

**原則: JS が 1 行も動かなくても、文書は完全に読める。**

JS が担当するのは以下だけで、いずれも文書の内容そのものではない。

| 機能 | JS が落ちたとき |
| --- | --- |
| 目次の生成とスクロール追従 | サイドバーが空になる（CSS の `:empty` で消える） |
| 見出しアンカー | `#` リンクが出ないだけ |
| コードのコピーボタン | 手で選択してコピーできる |
| 表の横スクロール化 | 表は表示される |
| Mermaid / 数式 / ハイライト | ソースがそのまま読める |
| 段落・リスト項目レビュー | コメント操作が出ず、文書は通常どおり読める |

CDN 依存のライブラリ（Mermaid・KaTeX・highlight.js）は
**必要なページでだけ、必要になった時点で**読み込む。
図も数式もない文書では 1 バイトも取りに行かない。
読み込みに失敗しても `try / catch` で握りつぶし、コンソール警告だけ残して本文は生きる。

### 目次を HTML に書かせない理由

AI に目次まで書かせると、本文を書き進める過程で見出しが変わったときに
目次との不整合が必ず起きる。しかも不整合は目視でしか見つからない。
**目次は本文から導出されるべき情報**なので、生成を実行時に寄せた。
AI が書くのは `<h2>` と `<h3>` だけでよい。

### レビューを別ファイルにした理由

人間の指摘は本文そのものではなく、次の編集入力である。そこでレビュー UI は段落とリスト項目の DOM を
実行時に装飾するだけにし、送信内容は `<HTML名>.review.json` へ分離した。元 HTML を書き換えない
ため、レビュー中の資料を壊さず、AI は本文と指摘を独立した入力として扱える。

下書きは `localStorage` に随時保存する。確定保存は `data-overmind` の同一オリジン API だけが
担当し、元文書とレビューのハッシュが一致しない場合は上書きを拒否する。一般的な静的サーバーや
`file://` では API が存在しないので、既定の `auto` 設定は UI ごと静かに無効化される。

### 見出し id を日本語のまま残す理由

`slugify()` はローマ字変換をしない。「背景と課題」は `背景と課題` のまま id になる。
機械的な音写は品質が安定せず、変換規則を追えなくなる。
URL 上はパーセントエンコードされるが、ブラウザの表示では日本語に戻るため
リンクを共有したときの可読性はむしろ高い。

---

## 5. 分離と単一ファイルを両立させる

同じ HTML が 2 通りの姿を取れるようにした。

- **分離配置**（`assets/doc.css` + `assets/doc.js` を `link` / `script` で参照）
  → 複数の文書でスタイルを共有し、1 か所の修正が全文書に反映される
- **単一ファイル**（`scripts/build-standalone.mjs` で CSS/JS をインライン化）
  → メール添付・Slack 貼り付け・アーカイブ。1 ファイルで完結する

**この両立を成立させるための制約が「CSS と JS に外部ファイルを参照させない」こと。**
`@import` も相対パスの画像も使っていないので、インライン化は
`<link>` を `<style>` に、`<script src>` を `<script>` に置換するだけで済む。
ビルドツールも依存パッケージもいらない、100 行程度の Node スクリプトで足りる。

Mermaid・KaTeX・highlight.js だけは CDN 参照のまま残す。
これらを埋め込むと 1 ファイルが数 MB になり、単一ファイル化の利点である
「軽く配れる」が失われる。**インライン化するのは自作分だけ**という線を引いた。

---

## 6. 画面と紙を同じ資産で賄う

日本の業務ドキュメントは印刷・PDF 化される確率が高い。
そこで印刷用スタイルを「おまけ」ではなく設計対象に含めた。

- `@page { size: A4; margin: 18mm 16mm }` — A4 を既定に置く
- 目次・コピーボタン・テーマ切替・見出しアンカーは `display: none`
- 見出し直後の改ページを禁止（`break-after: avoid`）
- コードブロック・表・コールアウト・図の途中改ページを禁止（`break-inside: avoid`）
- 表のヘッダ行をページ跨ぎで繰り返す（`thead { display: table-header-group }`）
- 外部リンクは `a[href^="http"]::after` で URL を併記する
- 紙では折り返せないコードだけ `white-space: pre-wrap` に切り替える
- 文字色をトークンごと純黒に寄せ、レーザープリンタでの潰れを避ける

`.page-break` クラス 1 つだけ、明示的な改ページ用に残してある。

---

## 7. アクセシビリティを既定値に埋め込む

書き手が意識しなくても最低限が満たされるよう、CSS 側に寄せた。

- 本文色とのコントラストは light / dark とも 4.5:1 以上を確保
- `:focus-visible` にのみアウトラインを出す（マウス操作では邪魔をしない）
- `prefers-reduced-motion` でアニメーションとスムーススクロールを無効化
- 横スクロールする表は `tabindex="0"` + `role="region"` でキーボード操作可能に
- 目次の現在位置は色だけでなく左罫と太字でも示す
- 本文へのスキップリンクを標準で置く

コールアウトの種別を色だけで区別していないのも同じ理由で、
`data-title` に「注意」「補足」と文字が入る前提にしてある。

---

## 8. 意図的に入れなかったもの

| 見送ったもの | 理由 |
| --- | --- |
| CSS フレームワーク（Tailwind 等） | ビルド工程が要る。単一ファイル配布と両立しない |
| Web フォント | ネットワークに依存する。日本語フォントは数 MB になる |
| 折りたたみ式サイドバーやモバイルドロワー | JS の分量に見合わない。狭い画面では帯に畳めば足りる |
| ページ内検索 UI | ブラウザの <kbd>Ctrl</kbd>+<kbd>F</kbd> で足りる |
| 印刷時の自動ページ番号・目次ページ | CSS だけでは安定して実現できない |
| ダークテーマの既定有効化 | 印刷・スクリーンショット共有でライトが無難 |
| 複数のカラーバリエーション | 選択肢は AI の出力を不安定にする。1 つに絞る |

最後の行が、このテンプレート全体を貫く判断基準になっている。
**選択肢を与えないほうが、AI の出力は安定する。**
